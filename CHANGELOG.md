# 更新说明

## v0.2.0 — 2025-11-13

### 功能与界面
- 顶部横幅改为盒装布局（`max-w-[1536px] mx-auto px-4`），与主内容宽度对齐。
- 底部横幅（页脚渐变背景）改为盒装布局，统一视觉边界并加入轻度圆角与阴影。
- 摄影版 Banner 继续保留玻璃标题卡与朱红判子，整体风格一致。
- Banner 高度优化：桌面 `42vh`、移动 `30vh`，更紧凑、更易读。

### 宽度统一
- 统一主要内容、顶部横幅与底部横幅到 `1536px` 最大宽度。
- 地图模块去除额外的 `max-width: 1400px` 限制，改为随主容器对齐。

### SEO 与分享
- `og:image` 与 `twitter:image` 更新为 Unsplash 官方直链（裁剪为 `1200x630`），提高兼容性与加载稳定性。
- 更新 `alt` 文本，提升无障碍与分享信息清晰度。

### 预览与验证
- 本地预览无浏览器错误，跨域图片加载正常。
- 顶部与底部的视觉宽度已与中间模块对齐。

### 备注
- 后续可以根据偏好微调：圆角（当前约 8px）、阴影强度、横向内边距（当前 `px-4`）、Banner 高度（如 `40vh`）。